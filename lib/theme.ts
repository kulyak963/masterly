/**
 * Единая дизайн-тема Mastersly — цвета, шрифты, отступы, скругления, тени.
 *
 * 2026-08-27, второй проход: полная смена направления на «Табло вылета» —
 * тёмное расписание/табло, а не «тёмный SaaS с золотом» (тот вид читался
 * как типовой ИИ-сгенерированный дизайн). Раз все страницы уже читают
 * цвета/шрифты только отсюда (см. первый проход), смена направления — это
 * правка одного файла, а не двадцати.
 *
 * Имена токенов (`gold`, `blue`, `grn`, `purp`...) оставлены прежними, чтобы
 * не перевязывать импорты во всех файлах — поменялись только значения и то,
 * что они теперь означают («gold» = янтарный цвет символов на табло, а не
 * металлическое золото).
 */

export const bg0 = '#0D0D0F' // основной фон — нейтрально-чёрный, без синего оттенка
export const bg1 = '#161618' // приподнятая панель (сайдбар, карточки)
export const bg2 = '#1D1D20' // вложенная поверхность (hover строки, канбан-карточка)
export const line = '#28282B' // линия сетки табло — сплошная, не полупрозрачная

export const t1 = '#ECEAE2' // основной текст — тёплый белый на холодном фоне
export const t2 = '#8C94A0' // приглушённый текст
export const t3 = '#565D68' // третичный текст / плейсхолдеры

export const gold = '#F2A93B' // янтарный — цвет символов сплит-флап дисплея, главный акцент
export const blue = '#6FA8C9' // «подтверждено» — как индикатор рейса по расписанию
export const red = '#D4776A' // срочность / отказ — тёплый тревожный, не кислотный
export const grn = '#5FAE72' // «оффер» / успех — приглушённый сигнальный зелёный
export const purp = '#8D8FE0' // «амбициозная» категория — холодный индиго, 4й отличимый акцент
export const amb = '#B98552' // вторичный акцент — тон багажной бирки/крафт-бумаги

export const sans = "'Overpass', sans-serif" // весь текст — заголовки и тело
export const serif = "'Overpass', sans-serif" // старое имя токена, то же семейство: никакого декоративного серифа
export const mono = "'Overpass Mono', monospace" // цифры, лейблы, статусы — как на настоящем табло

/** Шкала отступов в px. */
export const space = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  '2xl': 24,
  '3xl': 32,
  '4xl': 40,
  '5xl': 48,
  '6xl': 64,
  '7xl': 80,
} as const

/** Шкала скруглений в px — табло механическое, поэтому заметно площе прежнего. */
export const radius = {
  xs: 2,
  sm: 3,
  md: 4,
  lg: 6,
  xl: 8,
  pill: 100,
} as const

/** Готовые тени — тёмные и плоские, без тёплого «премиального» свечения. */
export const shadow = {
  sm: '0 2px 8px rgba(0,0,0,.4)',
  md: '0 8px 24px rgba(0,0,0,.45)',
  lg: '0 20px 48px rgba(0,0,0,.55)',
  glow: (color: string) => `0 0 0 3px ${color}25`,
} as const
