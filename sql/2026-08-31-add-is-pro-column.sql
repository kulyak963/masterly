-- Флаг "pro" на профиле — по просьбе Дениса, чтобы можно было смотреть
-- контент за платным замком (гайды по стипендиям, дедлайны в Таймлайне/
-- Journey) на своём аккаунте без реального платежа. Платёжного процессора
-- в проекте всё ещё нет — это ручной/админский флаг, включается только
-- через Supabase (см. scripts/set-pro.mjs), никакого UI для покупки нет.
--
-- НЕ применено в Supabase — выполнить вручную через SQL Editor (DDL нельзя
-- через supabase-js, тот же паттерн, что и с колонкой student_sentiment
-- 2026-08-28).

alter table profiles
  add column if not exists is_pro boolean not null default false;
